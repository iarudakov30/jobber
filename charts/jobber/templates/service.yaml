apiVersion: v1
kind: Service
metadata:
  name: {{ include "jobber.fullname" . }}
  labels:
    {{- include "jobber.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jobber.selectorLabels" . | nindent 4 }}
