{{- if .Values.auth.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: auth-http
  labels:
    app: auth
spec:
  type: LoadBalancer
  selector:
    app: auth
  ports:
    - protocol: TCP
      port: {{ .Values.auth.port.http }}
      targetPort: {{ .Values.auth.port.http }}
{{- end }}