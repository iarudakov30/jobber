apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jobber.fullname" . }}-test-connection"
  labels:
    {{- include "jobber.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jobber.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
